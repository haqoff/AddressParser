//1.В Utils создать класс RegexGroup для работы с Regex
ctor(List<AOType> параметры группы);

методы:
public List<Coll> Execute(string regexPattern)
{
	Regex();
}

Учесть след вариации
.....[тип обьекта с ] одно слово.


struct Coll:{
	Match,
	AOType
}
//